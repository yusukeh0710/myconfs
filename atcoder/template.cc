#include <bits/stdc++.h>
#include <boost/format.hpp>
using namespace std;
using LL = long long;
using boost::format;
namespace r = std::ranges;
auto range (int n) { return std::views::iota(0, n); }
auto range (int s, int e) { return std::views::iota(s, e+1); }

#define int long long int
#define AUTO [[maybe_unused]] auto
// constexpr int kMod = 1000000007;
constexpr int kMod = 998244353;

signed main() {
}
